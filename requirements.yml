---
collections:
  - name: community.general
    # Match Ludus' pinned collection. Newer releases moved proxmox_kvm and can
    # break Ludus range deployment before this role is reached.
    version: 10.3.0
